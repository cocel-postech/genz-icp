# Parameter set for indoor environments
# If this config doesn't work stably in your env., please refer to parameter tuning guide in README.md

# topic: (set in launch file or command line)
data:
  deskew: false
  max_range: 100.0
  min_range: 0.3
mapping:
  voxel_size: 0.3
  desired_num_voxelized_points: 2000
  planarity_threshold: 0.2
  max_points_per_voxel: 1
adaptive_threshold:
  initial_threshold: 2.0
  min_motion_th: 0.1
registration:
  max_num_iterations: 100
  convergence_criterion: 0.0001
